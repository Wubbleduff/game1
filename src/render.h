#pragma once

#include "common.h"

void render_world_rect(
        const f32 pos_x,
        const f32 pos_y,
        const f32 w,
        const f32 h,
        const f32 r,
        const f32 g,
        const f32 b,
        const f32 a);

